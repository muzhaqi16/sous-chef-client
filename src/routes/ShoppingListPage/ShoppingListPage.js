import React, { Component } from 'react'

export default class ShoppingListPage extends Component {
    render() {
        return (
            <section>
                ShoppingListPage
            </section>
        )
    }
}
